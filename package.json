{
  "name": "trusty-reviews-loyalty",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "georg",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
